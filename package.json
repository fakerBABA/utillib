{
  "name": "util-lib",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rm dist/bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "parcel index.html --no-source-maps  --out-dir=dist --open",
    "build": "parcel build index.js --no-source-maps"
  }
}